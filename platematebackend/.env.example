# =============================================================================
# PlateMate Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your actual values
# IMPORTANT: Never commit .env to version control - it contains sensitive keys
# =============================================================================

# -----------------------------------------------------------------------------
# Database Configuration (Required)
# -----------------------------------------------------------------------------
# PostgreSQL connection string for Neon Database or any PostgreSQL instance
# Format: postgresql://username:password@host:port/database?sslmode=require
# Example: postgresql://user:pass@ep-cool-name-123456.us-east-2.aws.neon.tech/neondb?sslmode=require
DATABASE_URL=postgresql://username:password@host:port/database

# -----------------------------------------------------------------------------
# AI Service API Keys (Required for food analysis features)
# -----------------------------------------------------------------------------
# OpenAI API key for GPT-4 food recognition and analysis
# Get yours at: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-...

# Google Gemini API key (alternative/fallback AI provider)
# Get yours at: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=

# Hugging Face API key (optional - used for experimental models)
# Get yours at: https://huggingface.co/settings/tokens
HUGGINGFACE_API_KEY=

# -----------------------------------------------------------------------------
# Mobile App Configuration (Required for Android/iOS builds)
# -----------------------------------------------------------------------------
# Full URL to your backend API server (for production mobile app builds)
# When building for mobile (APK/IPA), this MUST be set to your live server URL
# Examples:
#   - Replit: https://your-repl-name.replit.app
#   - Custom domain: https://api.platemate.com
#   - Local testing: http://192.168.1.100:5000
# Leave blank for web-only development (uses relative URLs automatically)
VITE_API_BASE_URL=https://your-backend-url.com

# Mobile app security token (required for native app API access)
# Generate a random secure token: openssl rand -base64 32
APP_ACCESS_TOKEN=

# RevenueCat API keys for subscription management (Android)
# Get yours at: https://app.revenuecat.com/
VITE_REVENUECAT_ANDROID_API_KEY=

# -----------------------------------------------------------------------------
# External Service APIs (Optional)
# -----------------------------------------------------------------------------
# USDA FoodData Central API key (optional - enhances nutrition data)
# Get yours at: https://fdc.nal.usda.gov/api-key-signup.html
USDA_API_KEY=

# -----------------------------------------------------------------------------
# Server Configuration (Optional - has sensible defaults)
# -----------------------------------------------------------------------------
# Port for the Express server (default: 5000)
PORT=5000

# Node environment (development | production)
NODE_ENV=development

# Session secret for cookie encryption (auto-generated if not set)
# Generate: openssl rand -hex 32
SESSION_SECRET=

# Upload directory for temporary files (default: uploads/ or /tmp/uploads in production)
UPLOAD_DIR=uploads

# -----------------------------------------------------------------------------
# Replit-Specific Variables (Only needed if deploying on Replit)
# -----------------------------------------------------------------------------
# These are automatically set by Replit - no need to configure locally
# REPL_ID=
# REPLIT_DEPLOYMENT=
# REPLIT_DOMAINS=
# ISSUER_URL=

# -----------------------------------------------------------------------------
# Object Storage (Auto-configured on Replit)
# -----------------------------------------------------------------------------
# These are automatically set if using Replit's object storage
# DEFAULT_OBJECT_STORAGE_BUCKET_ID=
# PUBLIC_OBJECT_SEARCH_PATHS=
# PRIVATE_OBJECT_DIR=

# =============================================================================
# Quick Start Guide
# =============================================================================
# 1. Copy this file: cp .env.example .env
# 2. Fill in DATABASE_URL with your PostgreSQL connection string
# 3. Add OPENAI_API_KEY or GEMINI_API_KEY for AI features
# 4. For mobile builds: Set VITE_API_BASE_URL to your server URL
# 5. Run: npm install && npm run dev
# =============================================================================
